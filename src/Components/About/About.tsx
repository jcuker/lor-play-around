export default function About() {
   return (
      <div className="flex flex-col justify-center items-center">
         <div className="flex flex-col justify-center items-center mb-12">
            <div className="text-gray-100">
               For each selected region, cards will be displayed that one should
               consider to play around. The list is exhaustive - it is up to the
               reader to know if the meta decks include the cards on the list.
            </div>
         </div>
      </div>
   );
}
